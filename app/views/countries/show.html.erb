<p id="notice"><%= notice %></p>

<p>
  <strong>Name,location,description:</strong>
  <%= @country.name,location,description %>
</p>

<%= link_to 'Edit', edit_country_path(@country) %> |
<%= link_to 'Back', countries_path %>
